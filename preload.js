// preload.js (can be an empty file for this simple wrapper)
// This script runs before the web page is loaded into the browser window.
// It has access to both DOM APIs and a node.js environment.
console.log('Preload script loaded.');